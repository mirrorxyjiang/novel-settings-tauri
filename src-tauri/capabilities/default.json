{
  "$schema": "https://schema.tauri.app/config/2",
  "identifier": "default",
  "description": "应用主窗口的默认权限集合",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
